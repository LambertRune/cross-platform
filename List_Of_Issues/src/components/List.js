import React from 'react'
import { Text } from 'react-native'
import {  issues } from './issues'

function list() {
    return({
        
    }
}

const First = () => {    
    return(
        issues.map({
            
        })
    )
}

export default First